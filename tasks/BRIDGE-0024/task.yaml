schema_version: '1.0'
kind: bridge_task
bridge_task_id: BRIDGE-0024
project_id: codex-control-bridge
title: Web-UI - Aktionen selbst committen und pushen
description: Web-UI um git commit + git push nach jeder erfolgreichen Aktion (copied/archive/finish)
  erweitern. Abgesichert durch Datei-Whitelist-Pruefung (fail-closed bei unerwarteten
  Aenderungen), Branch-main-Pruefung und kategorischem force-push-Verbot. SECURITY-MODEL.md
  Abschnitt 5a entsprechend erweitern. Details in work-packages/BRIDGE-024.md.
task_class: FEATURE
repository: Codex-Control-Bridge
branch: main
git:
  expected_head: b129773
permissions:
- WORKTREE_WRITE
- TEST_EXECUTION
- GIT_PUSH
depends_on:
- BRIDGE-0023
acceptance_criteria:
- git status --porcelain wird vor jedem Commit gegen eine feste Datei-Whitelist pro
  Aktionstyp geprueft; unerwartete Aenderungen blocken den Commit vollstaendig
- 'Branch-Pruefung: Commit/Push nur auf main, sonst Abbruch mit Klartext-Fehler'
- --force/--force-with-lease kommt im gesamten neuen Code nicht vor
- Erfolgreiche Aktion erzeugt genau einen Commit mit genau den erwarteten Dateien
  und pusht ihn (im Test gegen ein lokales bare Repo verifiziert)
- 'Push-Fehlschlag wird transparent gemeldet (pushed: false + Fehlertext), ohne Crash'
- Store-Erfolg und Git-Fehler werden getrennt und wahrheitsgemass gemeldet
- Persistenter Aktions-Log (BRIDGE-023) zeigt das Git-Ergebnis pro Eintrag
- SECURITY-MODEL.md Abschnitt 5a um neue Faehigkeit erweitert
- Bestehende Tests weiterhin gruen
- Neue Tests (WebUiGitActionTests) gruen inkl. Fail-closed- und Push-Fehlschlag-Fall
- Alle Tests gruen (bestehende Basis + neue), frischer Klon verifiziert
- Commit gepusht (GIT_PUSH-Berechtigung im Profil)
status: WAITING_FOR_COPY_TO_CONTROL
created_at: '2026-09-10T00:00:00Z'
created_by: browser-claude
