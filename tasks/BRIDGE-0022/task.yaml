schema_version: '1.0'
kind: bridge_task
bridge_task_id: BRIDGE-0022
project_id: codex-control-bridge
title: runner.resume() um REVIEW_REQUIRED erweitern
description: 'runner.py _RESUME_FROM erlaubt aktuell nur INTERRUPTED und WAITING_FOR_RESUME
  als Ausgangszustand fuer bridge run resume, obwohl schemas/state-model.yaml den
  Uebergang REVIEW_REQUIRED -> RUNNING ausdruecklich zulaesst. Fund aus BRIDGE-0020
  RUN-02 (fail-closed nach CLAUDE.md Regel 5). Fix: REVIEW_REQUIRED zu _RESUME_FROM
  hinzufuegen, Docstring anpassen, Tests fuer den neuen Pfad und Regressionstest fuer
  weiterhin verbotene Zustaende. APPROVAL_REQUIRED->RUNNING bewusst nicht im Scope,
  da aktuell kein Bedarf.'
task_class: BUGFIX
repository: Codex-Control-Bridge
branch: main
permissions:
- WORKTREE_WRITE
- TEST_EXECUTION
- GIT_PUSH
depends_on: []
acceptance_criteria:
- bridge run resume BRIDGE-0020 --actor claude-code funktioniert aus dem Zustand REVIEW_REQUIRED
  (manueller Nachtest gegen den echten Auftrag)
- _RESUME_FROM enthaelt REVIEW_REQUIRED zusaetzlich zu INTERRUPTED und WAITING_FOR_RESUME,
  keine weiteren Zustaende
- resume() aus nicht erlaubten Zustaenden scheitert weiterhin mit RunnerError (Regressionstest)
- bestehendes Verhalten fuer INTERRUPTED/WAITING_FOR_RESUME unveraendert
- alle Tests gruen (bestehende Basis + neue), frischer Klon verifiziert
- Commit gepusht (GIT_PUSH-Berechtigung im Profil)
status: ARCHIVED
created_at: '2026-09-09T00:00:00Z'
created_by: browser-claude
