schema_version: "1.0"
kind: bridge_project_profile
project_id: wetter-app
description: Wetter-App (zippeliniot/wetter-app) - gesteuert ueber Claude Code (Windows-nativ), menschlich kontrolliert.
repository: wetter-app
default_branch: main
task_prefix: WETTER
read_only: false
executor: claude-code
controller: human
review_roles:
  lead: anthropic
  support: openai
github_repo: zippeliniot/wetter-app
allowed_machines: [HAM11, DES11]
git_policy:
  allow_push: true
  allow_merge: false
  allow_force_push: false
  protected_branches: [main]
test_policy:
  command: null
  required: false
handover_policy:
  branch: main
  require_clean: true
migration_policy:
  allow_production: false
